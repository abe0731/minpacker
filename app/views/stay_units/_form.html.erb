<%= form_with(model: stay_unit, local: true) do |form| %>
  <% if stay_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stay_unit.errors.count, "error") %> prohibited this stay_unit from being saved:</h2>

      <ul>
      <% stay_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
